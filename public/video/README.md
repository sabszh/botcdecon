Expected background loop assets:

- `background-loop.mp4`
- optionally `background-loop.webm`
- `background-poster.jpg`

`BackgroundStage` will automatically use the first available loop asset and
fall back to the existing Three scene when no video asset is present.
